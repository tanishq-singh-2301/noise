import Noise from "@/components/noise";

const Home = () => {
    return (
        <div className="min-h-visible h-full w-full">
            <main className="h-full w-full flex justify-center items-center bg-neutral-100">
            	<Noise />
			</main>
        </div>
    )
}

export default Home;
